{
  "name": "zkip",
  "version": "0.1.0",
  "scripts": {
    "docs:dev": "vocs dev",
    "docs:build": "npx playwright install --with-deps chromium && vocs build",
    "docs:preview": "vocs preview"
  },
  "dependencies": {
    "vocs": "latest"
  }
}
